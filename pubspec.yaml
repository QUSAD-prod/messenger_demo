name: messenger_demo
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  auto_route: ^10.0.1
  equatable: ^2.0.7
  firebase_core: ^3.12.1
  flutter_bloc: ^9.1.0
  get_it: ^8.0.3
  hive_flutter: ^1.1.0
  talker_flutter: ^4.6.14

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
